import { isFavorite } from "../../helpers/isFavorite";

describe("pruebas en isFavorite", () => {
  test("should first", async () => {
    const Song = [];
    const Favorite = false;
    // expect(isFavorite(Song, Favorite)).toEqual(true);
  });
});
